/**
 * ${mod.description}
 * js命名空间
 * ${mod.jsns}
 * css命名控件.${mod.cssns}
 * 模块初始入口：
 * ${mod.jsns}.init(cfg)
 */
ns("${mod.jsns}",function(){
    return {
        init:function(cfg){
            //...
        }
    }
})