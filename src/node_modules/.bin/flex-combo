#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../flex-combo/bin/flex-combo" "$@"
  ret=$?
else 
  node  "$basedir/../flex-combo/bin/flex-combo" "$@"
  ret=$?
fi
exit $ret
